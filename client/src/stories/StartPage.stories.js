import React from "react";
import StartPage from "../pages/StartPage";

export default {
  title: "Pages/StartPage",
  component: StartPage,
};

export const Start = (args) => <StartPage {...args} />;
